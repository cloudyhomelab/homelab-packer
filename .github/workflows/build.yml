name: Runner Sanity Test

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: [self-hosted]

    steps:
      - name: checkout repository
        uses: actions/checkout@v4

      - name: validate
        run: |
          make validate
